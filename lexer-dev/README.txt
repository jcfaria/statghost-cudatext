lexer-dev/ — workshop for CudaText LCF packs
=======================================================
Updated: 2026-08-06
Repo: jcfaria/statghost-cudatext (canonical)

Edit here, validate in CudaText `data/lexlib`, then copy into:
  - this repo `lexlib/` (promoted)
  - STATghost `lexer/` (Console beside EXE)
  - CudaText lab installs (GitHub app + portable Dropbox if used)

Do not commit `_loadtest/` (local LCF load probe).

- `R/` — STATghost slim R (Notes / Programing / Datasets)
- `Python/` — stock CudaText Python + Notes `#.` / `#..` / `#...`
- `Julia/` — stock CudaText Julia + Notes
- `txt/` — Text file Notes sample (lab)

SampleText in each pack leads with Note1 / Note2 / Note3 lines.
